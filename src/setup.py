from setuptools import setup, find_packages

setup(
    name="flegmgui",  # The name you will use to install it
    version="0.1.0",
    packages=find_packages(), # Automatically finds your inner folder
)
